useDynLib(sdcMicro)

import(robustbase, gWidgetsRGtk2, tcltk)
importsFrom(car, scatterplotMatrix, box.cox.powers, box.cox)
importsFrom(robustbase, covMcd)
importsFrom(cluster, clara, pam, fanny)
importsFrom(stats, kmeans)
importsFrom(mclust, Mclust)
importsFrom(MASS, princomp)
importsFrom(e1071, impute)

export(
    addNoise,
    dataGen,
    dRisk,
    dRiskRMD,
    dUtility,
    freqCalc,
    globalRecode,
    indivRisk,
    localSupp,
    localSupp2,
    localSupp2Wrapper,
    microaggregation,
    plot.indivRisk,
    plot.localSupp2,
    plotMicro,
    pram,
    print.freqCalc,
    print.indivRisk,
    print.localSupp2,
    print.micro,
    print.pram,
    sdcGUI,
    summary.freqCalc,
    summary.micro,
    summary.pram,
    swappNum,
    topBotCoding,
    valTable
)

S3method(print, indivRisk)
S3method(print, freqCalc)
S3method(print, localSupp2)
S3method(print, micro)
S3method(print, pram)
S3method(summary, freqCalc)
S3method(summary, micro)
S3method(summary, pram)
S3method(plot, indivRisk)
S3method(plot, localSupp2)
